#include "ui.h"

lor_UIPtr buildImageViewerUI(void* pUserData, lor_AllocatorPtr allocator) {
    
    // lor_UIElementEmpty dummyRect = { LOR_STRUCT_UI_ELEMENT_EMPTY };
    // dummyRect.sWidth = 100;
    // dummyRect.sHeight = 100;
    // dummyRect.sFillColor = LOR_PACK_RGBA(255, 0, 0, 255);


    // lor_UILayout rootLayout = { LOR_STRUCT_UI_LAYOUT };
    // rootLayout.sLayoutType = LOR_UI_LAYOUT_TYPE_FLOW;
    // rootLayout.sChildrenCount = 1;
    // rootLayout.pChildren = &dummyRect;

    // lor_UIConfig uiConfig = { LOR_STRUCT_UI_CONFIG };
    // uiConfig.pRoot = &rootLayout;

    // lor_UIPtr pUI = lorBuildUI(&uiConfig, allocator);
    // if (pUI == NULL) {
    //     fprintf(stderr, "Failed to build UI.\n");
    //     return NULL;
    // }
    
    return NULL;
}
